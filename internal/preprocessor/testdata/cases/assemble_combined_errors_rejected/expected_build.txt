# Each line below is an independent required substring of the build output.
# This call has TWO problems; the resolver must list both in one diagnostic.
duplicate provider for *Config
missing recipe for *Cache — needed by #3 (newServer)
What the resolver sees:
- *Server <- #3 (newServer) [fn]
- input *Cache ?? (no recipe provides this)
Providers supplied: #1→*Config, #2→*Config, #3→*Server